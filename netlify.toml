[build]
  command = "pnpm build"
  publish = "client/dist"
  base = "client"

[dev]
  command = "pnpm dev"
  port = 3000
  targetPort = 3000
  publish = "dist"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
